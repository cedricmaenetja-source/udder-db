# udder-db

npm install cookie

# Build SCSS
```bash
npm run build:css
```

# Deploy locally
```bash
vercel dev
```

# Deploying to prod

```bash
npm run build // always run build first before deploying to prod
npx vercel --prod
```